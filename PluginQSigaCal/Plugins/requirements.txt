# QSIGA-CAL - Python Dependencies
# Install via OSGeo4W Shell (see README.md for instructions)
#
# IMPORTANT: numpy and scipy must be these exact versions
# (required for numba and pysheds compatibility)

# Core scientific computing
numpy==1.26.4
scipy==1.13.0
pandas>=1.1.0

# Optimization and calibration
spotpy>=1.5.14

# Plotting and visualization
matplotlib>=3.3.0
seaborn>=0.11.0

# Excel / CSV file handling
openpyxl>=3.0.0
xlrd>=1.2.0

# Geospatial processing
rasterio>=1.2.0
geopandas>=0.9.0
shapely>=1.7.0
pyproj>=3.0.0
fiona>=1.8.0
pysheds>=0.3.0

# Numerical acceleration
numba>=0.56.0

# Statistics
statsmodels>=0.12.0
scikit-learn>=0.24.0

# HTTP requests
requests>=2.25.0

# Note: GDAL (osgeo), PyQt5, and numpy are bundled with QGIS.
# The pip install command in README.md overrides numpy to the required version.
